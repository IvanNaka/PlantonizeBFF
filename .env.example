# Exemplo de configuração de variáveis de ambiente para o BFF
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Porta do servidor (opcional, padrão: 3000)
# No Azure, essa variável é definida automaticamente pelo App Service
PORT=3000

# URL do microserviço de Plantões
# Desenvolvimento local: http://localhost:5219
# Produção: https://seu-servico-plantoes.azurewebsites.net
MICRO_PLANTAO_URL=http://localhost:5219

# URL do microserviço de Notas Fiscais
# Desenvolvimento local: http://localhost:5113/api
# Produção: https://seu-servico-nf.azurewebsites.net/api
MICRO_NF_URL=http://localhost:5113/api

# URL da Azure Function para envio de e-mails (opcional)
# Deixe vazio se não usar a funcionalidade de e-mail
# Exemplo: https://sua-function.azurewebsites.net/api/enviar-email
FUNCTION_EMAIL_URL=

# Configurações opcionais para desenvolvimento
# Habilitar fallback automático entre http/https para microserviços locais
ALLOW_PROTOCOL_FALLBACK=true

# Desabilitar verificação de certificados TLS (apenas para desenvolvimento local!)
# NUNCA habilite isso em produção
ALLOW_INSECURE_TLS=false
